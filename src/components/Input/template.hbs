<input
  class="{{class}}"
  type="{{type}}"
  name="{{name}}"
  id="{{#if id}}{{id}}{{else}}{{name}}{{/if}}"
  {{#if placeholder}}placeholder="{{{placeholder}}}" {{/if}}
  {{#if data}} value="{{data}}"{{/if}}
  {{#if accept}} accept="{{accept}}" {{/if}}
/>
