<main class="form-wrapper">
    <form class="form {{classWrapper}}" action="#">
      <h1 class="form__header">{{title}}</h1>
      {{#each fields}}
        {{{this}}}
      {{/each}}
        {{{loginButton}}}
      <nav>
        {{{link}}}
      </nav>
      </a>
    </form>
</main>
